import { Theme } from "@mui/material";

export const styles = {
  menuText: {
    marginLeft: (theme: Theme) => theme.spacing(1.5),
    fontWeight: "600",
    fontSize: "16px",
  },
  divider: {
    marginTop: "60px !important",
  },
};
